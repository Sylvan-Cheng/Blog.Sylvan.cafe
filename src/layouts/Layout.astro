---
import { PUBLIC_GOOGLE_SITE_VERIFICATION } from "astro:env/client";
import { ClientRouter } from "astro:transitions";
import { SITE } from "@/config";
import { LOCALE_META, type Locale } from "@/i18n/config";
import { UI } from "@/i18n/ui";
import { resolveLocale, t } from "@/i18n/utils";
import { buildDocumentHeadModel } from "@/utils/documentHeadModel";
import { serializeJsonLd } from "@/utils/layoutSeo";
import "@/styles/global.css";
import "@fontsource/noto-sans/latin-400.css";
import "@fontsource/noto-sans/latin-700.css";
import "@fontsource/noto-sans/cyrillic-400.css";
import "@fontsource/noto-sans/cyrillic-700.css";
import "@fontsource/noto-sans-sc/chinese-simplified-400.css";
import "@fontsource/noto-sans-sc/chinese-simplified-700.css";
import "@fontsource/noto-sans-jp/japanese-400.css";
import "@fontsource/noto-sans-jp/japanese-700.css";
import "@fontsource/fira-code/latin-400.css";

import cyrillic400 from "@fontsource/noto-sans/files/noto-sans-cyrillic-400-normal.woff2";
// Preload font assets via Vite to get hashed URLs for <link rel="preload">
import latin400 from "@fontsource/noto-sans/files/noto-sans-latin-400-normal.woff2";
import latin700 from "@fontsource/noto-sans/files/noto-sans-latin-700-normal.woff2";
import jp400 from "@fontsource/noto-sans-jp/files/noto-sans-jp-japanese-400-normal.woff2";
import sc400 from "@fontsource/noto-sans-sc/files/noto-sans-sc-chinese-simplified-400-normal.woff2";

type Props = {
  title?: string;
  author?: string;
  profile?: string;
  description?: string;
  pubDatetime?: Date;
  modDatetime?: Date | null;
  hreflangs?: Record<string, string>;
  canonical?: boolean;
  alternateLinks?: boolean;
  robots?: string;
  locale?: Locale;
};

const locale = Astro.props.locale ?? resolveLocale(Astro.currentLocale);
const head = buildDocumentHeadModel({
  astro: Astro,
  locale,
  fontAssets: { cyrillic400, jp400, sc400 },
});
---

<!--
   _____       _
  / ____|     | |
 | (___  _   _| |_   ____ _ _ __
  \___ \| | | | \ \ / / _` | '_ \
  ____) | |_| | |\ V / (_| | | | |
 |_____/ \__, |_| \_/ \__,_|_| |_|
          __/ |
         |___/
-->
<!doctype html>
<html
  dir={SITE.dir}
  data-scheme={SITE.themeScheme}
  lang={LOCALE_META[locale].lang}
>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, viewport-fit=cover" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    {head.canonicalURL && <link rel="canonical" href={head.canonicalURL} />}
    {head.robots && <meta name="robots" content={head.robots} />}
    <meta name="generator" content={head.generator} />

    <!-- Preconnect to Umami analytics early -->
    <link rel="preconnect" href={SITE.analytics.umami.url} crossorigin />
    <link rel="dns-prefetch" href={SITE.analytics.umami.url} />

    <!-- Preconnect to Giscus comments -->
    <link rel="preconnect" href="https://giscus.app" crossorigin />

    <!-- Preload critical fonts to reduce CLS -->
    <link rel="preload" href={latin400} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={latin700} as="font" type="font/woff2" crossorigin />
    {
      head.scriptFont && (
        <link rel="preload" href={head.scriptFont} as="font" type="font/woff2" crossorigin />
      )
    }

    <!-- hreflang tags for multilingual SEO -->
    {
      head.hreflangLinks.map(({ lang, url }) => (
        <link
          rel="alternate"
          hreflang={lang}
          href={url}
        />
      ))
    }
    {
      head.xDefaultUrl && (
        <link
          rel="alternate"
          hreflang="x-default"
          href={head.xDefaultUrl}
        />
      )
    }

    <!-- General Meta Tags -->
    <title>{head.title}</title>
    <meta name="title" content={head.title} />
    <meta name="description" content={head.description} />
    <meta name="author" content={head.author} />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link rel="llms-txt" href="/llms.txt" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content={head.isArticle ? "article" : "website"} />
    <meta property="og:locale" content={LOCALE_META[locale].lang} />
    <meta property="og:site_name" content={SITE.title} />
    <meta property="og:title" content={head.title} />
    <meta property="og:description" content={head.description} />
    {head.canonicalURL && <meta property="og:url" content={head.canonicalURL} />}
    <meta property="og:image" content={head.socialImageURL} />

    <!-- Article Published/Modified time -->
    {
      head.pubDatetime && (
        <meta
          property="article:published_time"
          content={head.pubDatetime.toISOString()}
        />
      )
    }
    {
      head.modDatetime && (
        <meta
          property="article:modified_time"
          content={head.modDatetime.toISOString()}
        />
      )
    }

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    {head.canonicalURL && <meta property="twitter:url" content={head.canonicalURL} />}
    <meta property="twitter:title" content={head.title} />
    <meta property="twitter:description" content={head.description} />
    <meta property="twitter:image" content={head.socialImageURL} />

    <!-- Google JSON-LD Structured data (only for blog posts) -->
    {
      head.structuredData && (
        <script
          type="application/ld+json"
          is:inline
          set:html={serializeJsonLd(head.structuredData)}
        />
      )
    }

    <!-- Enable RSS feed auto-discovery -->
    <link
      rel="alternate"
      type="application/rss+xml"
      title={SITE.title}
      href={new URL(`${locale}/rss.xml`, Astro.site)}
    />

    <meta name="theme-color" content="" />

    {
      // If PUBLIC_GOOGLE_SITE_VERIFICATION is set in the environment variable,
      // include google-site-verification tag in the heading
      // Learn more: https://support.google.com/webmasters/answer/9008080#meta_tag_verification&zippy=%2Chtml-tag
      PUBLIC_GOOGLE_SITE_VERIFICATION && (
        <meta
          name="google-site-verification"
          content={PUBLIC_GOOGLE_SITE_VERIFICATION}
        />
      )
    }

    <ClientRouter />

    <!-- Umami Analytics (Partytown) -->
    <script is:inline type="text/partytown" src={`${head.analyticsUrl}/script.js`} data-website-id={head.umamiWebsiteId}></script>

    <!-- Minimal inline script to prevent FOUC - sets theme immediately -->
    <script is:inline define:vars={{ themeColors: head.themeColors }}>
      (function () {
        const initialColorScheme = ""; // "light" | "dark"
        const currentTheme = localStorage.getItem("theme");

        function getPreferTheme() {
          if (currentTheme) return currentTheme;
          if (initialColorScheme) return initialColorScheme;
          return window.matchMedia("(prefers-color-scheme: dark)").matches
            ? "dark"
            : "light";
        }

        const themeValue = getPreferTheme();

        // Set theme immediately to prevent flash
        document.firstElementChild?.setAttribute("data-theme", themeValue);
        localStorage.removeItem("theme-scheme");

        // 同步设置浏览器地址栏颜色，避免 Android Chrome 暗色模式闪白
        document.querySelector("meta[name='theme-color']")
          ?.setAttribute("content", themeColors[themeValue] || "");

        // 合并现有 API（视图过渡时保留 theme.ts 扩展的方法）
        window.theme = {
          ...(window.theme || {}),
          themeValue: themeValue,
          getTheme: () => window.theme.themeValue,
          setTheme: val => {
            window.theme.themeValue = val;
          },
        };
      })();
    </script>
    <slot name="head" />
  </head>
  <body data-reading-progress-label={t(UI.readingProgress, locale)}>
    <slot />

    <!-- Load full theme logic -->
    <script src="../scripts/theme.ts"></script>
  </body>
</html>
